/** @format */

const cfg = {
    headers: {
        'content-type': 'application/x-www-form-urlencoded;charset=UTF-8',
    },
    // @ts-ignore 无法解决编辑器报错提示
    baseUrl: `${import.meta.env.VITE_BASEURL}`,
};

export default cfg;
